name: coverage
description: Merge and upload code coverage

inputs:
  files:
    required: true

runs:
  using: "composite"
  steps:
    - run: echo Placeholder for code coverage upload
      shell: bash

    - name: Upload test coverage files
      uses: actions/upload-artifact@v2
      with:
        name: coverage
        path: |
          ${{ inputs.files }}